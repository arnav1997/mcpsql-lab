[project]
name = "mcp-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "psycopg[binary]>=3.1",
    "sqlparse>=0.5",
    "requests>=2.32",
    "python-dotenv>=1.1.1",
]

[tool.uv]
# no special settings needed